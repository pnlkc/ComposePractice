package com.example.sports.utils

/*
3. 새 디렉터리 utils를 만들고 새 파일 WindowStateUtils.kt를 만듭니다.
4. WindowStateUtils에 enum 클래스를 추가하여 서로 다른 contentType 두 개를 나타냅니다.
   ListOnly 및 ListAndDetail 유형으로 이름을 지정할 수 있습니다.
*/
enum class SportsContentType {
    LIST_ONLY, LIST_AND_DETAIL
}